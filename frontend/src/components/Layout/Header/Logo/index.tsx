import Link from "next/link";

const Logo: React.FC = () => {
  return (
    <Link href="/" className="inline-flex items-center text-3xl font-semibold tracking-normal text-midnight_text">
      Travixa
    </Link>
  );
};

export default Logo;
